package org.firstinspires.ftc.teamcode.Location;

public class Start {
    public enum CustomElement { // Determines the level at which the preloaded element will be scored
        LEFT,
        MID,
        RIGHT
    }

    public enum TeamColor { // Determines the directions of robot movement
        RED,
        BLUE
    }

    public enum StartLocation { // Determines what the robot will do to score points
        SPINNER,
        WAREHOUSE
    }
}