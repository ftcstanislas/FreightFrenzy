package org.firstinspires.ftc.teamcode.Location;

public class Start {
    public enum CustomElement {
        LEFT,
        MID,
        RIGHT
    }

    public enum TeamColor {
        RED,
        BLUE
    }

    public enum StartLocation {
        SPINNER,
        WAREHOUSE
    }
}